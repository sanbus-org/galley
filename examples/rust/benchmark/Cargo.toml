[package]
name = "galley-rust-benchmark"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "benchmark"
path = "../src/benchmark.rs"

[dependencies]
galley-bindings = { path = "../../../bindings/rust" }

[build-dependencies]
galley-bindings = { path = "../../../bindings/rust" }
