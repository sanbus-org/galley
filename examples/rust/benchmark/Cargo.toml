[package]
name = "galley-rust-benchmark"
version = "0.0.0"
edition = "2021"

[[bin]]
name = "benchmark"
path = "../src/benchmark.rs"

[dependencies]
galley = { path = "../../../bindings/rust" }

[build-dependencies]
galley = { path = "../../../bindings/rust" }
