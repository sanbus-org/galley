[project]
name = "compiler"
version = "0.0.1"
description = "LL, LR, and GLR parser generator emitting native Zig parsers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jinja2>=3.1.6",
]

[dependency-groups]
dev = [
    "graphviz>=0.21",
    "shtab>=1.8.0",
]
