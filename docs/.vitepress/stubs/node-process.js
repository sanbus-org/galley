// Browser stub: just enough shape for `typeof process` checks.
// No `versions.node`, so the adapter takes its browser branches.
const process = { env: {}, platform: "browser" };
export default process;
