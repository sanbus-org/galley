import{_ as a,o as t,c as r,a1 as n}from"./chunks/framework.BwhMLafE.js";const p=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md"}'),i={name:"architecture.md"};function o(s,e,c,l,d,h){return t(),r("div",null,[...e[0]||(e[0]=[n('<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><h2 id="table-of-contents" tabindex="-1">Table of Contents <a class="header-anchor" href="#table-of-contents" aria-label="Permalink to &quot;Table of Contents&quot;">​</a></h2><ul><li><a href="#overview">Overview</a></li><li><a href="#unified-no-lexer-design">Unified No-Lexer Design</a></li><li><a href="#native-call-stack-execution">Native Call-Stack Execution</a></li><li><a href="#syntax-error-recovery">Syntax-Error Recovery</a></li><li><a href="#dynamic-stack-overflow-recovery">Dynamic Stack-Overflow Recovery</a></li><li><a href="#dense-integer-node-pooling">Dense Integer Node Pooling</a></li><li><a href="#role-of-the-self-hosted-generator">Role of the Self-Hosted Generator</a></li><li><a href="#self-hosting">Self-Hosting</a></li></ul><hr><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Galley achieves parsing speeds tens to hundreds of times faster than traditional table-driven parser generators by fundamentally rethinking how parsers interact with memory and the CPU. Rather than interpreting state transitions at runtime, Galley directly encodes grammar semantics into compile-time Zig execution paths.</p><hr><h2 id="unified-no-lexer-design" tabindex="-1">Unified No-Lexer Design <a class="header-anchor" href="#unified-no-lexer-design" aria-label="Permalink to &quot;Unified No-Lexer Design&quot;">​</a></h2><p>Traditional parsers split execution into two passes: a lexer (tokenizer) that scans source text and allocates token objects on the heap, followed by a parser that consumes those tokens.</p><p>Galley eliminates the separate lexer pass entirely. Character matching and structural grammar reduction happen simultaneously in a single, unified pass over the source byte buffer. By avoiding token allocation and intermediate buffering, memory bus traffic is reduced by over 50%.</p><hr><h2 id="native-call-stack-execution" tabindex="-1">Native Call-Stack Execution <a class="header-anchor" href="#native-call-stack-execution" aria-label="Permalink to &quot;Native Call-Stack Execution&quot;">​</a></h2><p>In both generated LL recursive-descent and LR recursive-ascent parsers, Galley leverages the native CPU execution call stack as the grammar parsing stack.</p><p>Instead of dynamically allocating stack frame objects or pushing/popping state IDs in an array loop, grammar transitions compile directly into native machine function calls (<code>call</code> and <code>ret</code> instructions). This allows modern CPUs to fully utilize their hardware return address stacks (RAS) and branch prediction units, resulting in near-zero overhead state transitions.</p><hr><h2 id="syntax-error-recovery" tabindex="-1">Syntax-Error Recovery <a class="header-anchor" href="#syntax-error-recovery" aria-label="Permalink to &quot;Syntax-Error Recovery&quot;">​</a></h2><p>Generated LL and LR parsers are fail-fast by default: a mismatch records and prints one diagnostic, then returns <code>ParseError.SyntaxError</code>. Passing <code>with_error_recovery = true</code> to the generator enables recovery. Generated parsers expose <code>error_recovery_mode</code> as <code>.disabled</code>, <code>.automatic</code>, or <code>.explicit</code>, while retaining <code>is_error_recovery_enabled</code> for compatibility.</p><p>An enabled grammar without recovery annotations uses automatic recovery. If any LHS variable, production, or RHS variable occurrence carries a <code>!</code> synchronization annotation, the parser instead uses explicit-only recovery with no automatic fallback. An annotation records an exact terminal and whether synchronization resumes before it (preserving it) or after it (consuming it). Disabled generation keeps annotations inert and emits one warning.</p><p>An automatic-mode LL syntax mismatch transfers control to a generated cold handler. The handler prints the first diagnostic at an input position, searches ahead for the failing symbol&#39;s recovery candidates, and returns a neutral parser value. The parent then continues through its ordinary generated code, naturally exposing later grammar states as recovery points.</p><p>An automatic-mode LR mismatch uses the same bounded lookahead and position-based diagnostic suppression, with recovery candidates derived from the complete terminals accepted by the current LR state. Finding a candidate skips the invalid input and retries the state. If the state cannot resynchronize, an internal result unwinds one native LR frame and, when AST construction is enabled, its semantic value; the caller recognizes it and retries in its existing frame. This continues until the nearest viable state resumes or the initial state is exhausted. Unrecoverable end-of-input stops at the original diagnostic instead of inventing a terminal.</p><p>Explicit recovery separates mismatch detection from synchronization. Once a production is committed, the parser tries the active RHS occurrence, selected production, LHS variable, and then enclosing committed reductions. LR recovery annotations are stored separately from canonical LR items, so adding or removing them cannot change closures, states, actions, gotos, or state numbering. Explicit LR state calls carry a small linked frame containing the canonical state and incoming symbol; after a mismatch, the recovery planner combines those active frames with canonical closure metadata to resolve committed scopes without a second LR stack. For each annotated occurrence, bounded graph reachability checks whether any productive closure path can avoid it; the occurrence is active only when no surviving path can, so shared-prefix states do not activate speculative scopes. Enclosing <code>(frame, item)</code> lineages are likewise deduplicated into a finite graph, and occurrence, production, and LHS scopes become candidates only when they dominate every productive exit. Neither analysis enumerates or copies closure paths. For consecutive terminals on one target, selection is deterministic: earliest input offset, longest terminal, then annotation source order. A successful recovery attaches the winning target, terminal, and resume side to the existing diagnostic, neutral-completes the damaged variable, discards its partial semantic state, and skips its occurrence, production, and variable hooks. Message-hook invocation is deferred until the structured recovery context is finalized.</p><p>Automatic recovery does not use Zig errors for internal control flow: LL void parsers return normally, AST parsers return the invalid-node sentinel, and LR state functions return an internal recovery result when a frame must unwind. Explicit LL recovery instead propagates a private <code>ExplicitSyntaxRecovery</code> signal until a committed annotated boundary synchronizes or the public entry point converts it to <code>ParseError.SyntaxError</code>; explicit LR recovery carries the equivalent result through its state frames. A session-local target-and-position guard prevents a preserved terminal from repeatedly selecting the same explicit scope. Resynchronizing completes the current recovery and permits a later mismatch to be reported separately. Automatic LL recovery can neutral-complete a missing symbol at end-of-input, while explicit recovery requires a matching synchronization terminal.</p><p>Normal automatic-mode LL child calls retain the same <code>try parse_child(...)</code> shape. Eligible automatic recovery calls use <code>always_tail</code> on the LLVM and native AArch64 backends and fall back to ordinary calls on other backends. LR state calls inspect the returned recovery result, and each state uses its existing native frame rather than a second parser stack. Neither parser scans for synchronization terminals during normal shifts or reductions; recovery lookahead allocation happens only after a mismatch. For indentation-sensitive languages, the search distance counts parser input units, including generated indent and dedent symbols. Procedures may run on partial or later-discarded trees, so an AST from erroneous input is diagnostic data rather than a guaranteed-valid syntax tree.</p><p>Recovery-enabled parsers stop after 10 syntax errors by default. Runtime callers can set <code>ParseOptions.max_errors</code> and <code>ParseOptions.recovery_window</code>; recovery-enabled parser executables expose the same settings as <code>--max-errors</code> and <code>--recovery-window</code>.</p><hr><h2 id="dynamic-stack-overflow-recovery" tabindex="-1">Dynamic Stack-Overflow Recovery <a class="header-anchor" href="#dynamic-stack-overflow-recovery" aria-label="Permalink to &quot;Dynamic Stack-Overflow Recovery&quot;">​</a></h2><p>Leveraging the native CPU call stack introduces a potential risk when parsing deeply recursive structures (such as thousands of nested JSON arrays): exceeding the operating system thread stack limit.</p><p>To prevent crashes, Galley includes a runtime stack-overflow recovery mechanism. As parsing approaches the stack limit, the runtime intercepts execution and dynamically transitions to heap-backed continuation frames. This guarantees safety on arbitrarily deep input files while maintaining maximum bare-metal speed during normal execution depths.</p><hr><h2 id="dense-integer-node-pooling" tabindex="-1">Dense Integer Node Pooling <a class="header-anchor" href="#dense-integer-node-pooling" aria-label="Permalink to &quot;Dense Integer Node Pooling&quot;">​</a></h2><p>When AST construction is enabled, Galley avoids allocating individual nodes via the system heap (<code>malloc</code>). Instead, nodes are allocated from contiguous, preallocated memory pools (<code>ASTAllocator</code>).</p><p>Furthermore, AST nodes reference their parents, children, and siblings using compact integer indices (<code>u16</code> or bit-width defined by <code>--input-size</code>) rather than 64-bit pointers. This cuts AST memory consumption in half, ensures dense cache packing in CPU L1/L2 caches, and allows resetting the entire parser state between iterations in (O(1)) time simply by zeroing a counter.</p><hr><h2 id="role-of-the-self-hosted-generator" tabindex="-1">Role of the Self-Hosted Generator <a class="header-anchor" href="#role-of-the-self-hosted-generator" aria-label="Permalink to &quot;Role of the Self-Hosted Generator&quot;">​</a></h2><p>The grammar analysis engine is self-hosted in Zig. Galley ships an LL seed parser for its own grammar format in <code>languages/galley/_ll-parser.zig</code>; that parser is responsible for:</p><ol><li>Parsing the <code>.grm</code> definition files.</li><li>Computing FIRST, FOLLOW, and nullable sets.</li><li>Constructing deterministic LL(k) lookup tables or LR/LALR shift-reduce automatas.</li><li>Emitting highly optimized, zero-boilerplate Zig code (<code>_ll-parser.zig</code> and <code>_lr-parser.zig</code>).</li></ol><p>Because this step happens entirely ahead-of-time (AOT), the runtime Zig binary carries zero generator overhead. The original Python bootstrap generator was removed after commit <code>0190e40</code>.</p><hr><h2 id="self-hosting" tabindex="-1">Self-Hosting <a class="header-anchor" href="#self-hosting" aria-label="Permalink to &quot;Self-Hosting&quot;">​</a></h2><p>Galley ships with a formal specification of its own grammar syntax (<code>languages/galley</code>). The tracked LL seed parser can parse <code>.grm</code> files and generate both LL and LR parsers from them. The Galley LR parser stays generated/ignored and is used as a verification path rather than as a second bootstrap artifact.</p>',40)])])}const m=a(i,[["render",o]]);export{p as __pageData,m as default};
