import{_ as a,o as t,c as s,a1 as o}from"./chunks/framework.BwhMLafE.js";const u=JSON.parse('{"title":"AST Node Allocation Mechanics & Limitations (LL vs. LR)","description":"","frontmatter":{},"headers":[],"relativePath":"ast_node_allocations.md","filePath":"ast_node_allocations.md"}'),i={name:"ast_node_allocations.md"};function n(r,e,l,c,d,p){return t(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="ast-node-allocation-mechanics-limitations-ll-vs-lr" tabindex="-1">AST Node Allocation Mechanics &amp; Limitations (LL vs. LR) <a class="header-anchor" href="#ast-node-allocation-mechanics-limitations-ll-vs-lr" aria-label="Permalink to &quot;AST Node Allocation Mechanics &amp; Limitations (LL vs. LR)&quot;">​</a></h1><h2 id="table-of-contents" tabindex="-1">Table of Contents <a class="header-anchor" href="#table-of-contents" aria-label="Permalink to &quot;Table of Contents&quot;">​</a></h2><ul><li><a href="#1-structural-node-allocations-is_ast_enabled">1. Structural Node Allocations (<code>is_ast_enabled</code>)</a></li><li><a href="#2-terminal-ast-allocations-ast-for-terminals">2. Terminal AST Allocations (<code>ast-for-terminals</code>)</a><ul><li><a href="#ll-parser-generated-ast-suppressed-variants">LL Parser: Generated AST-Suppressed Variants</a></li><li><a href="#lr-parser-static-analysis-constraint">LR Parser: Static Analysis Constraint</a></li></ul></li></ul><hr><p>When building optimized parser generators, managing memory allocation in the Abstract Syntax Tree (AST) is critical. This document details the architectural differences, optimizations, and compiler-level limitations of AST node allocations in LL (top-down) and LR (bottom-up) parsers in this project.</p><hr><h2 id="_1-structural-node-allocations-is-ast-enabled" tabindex="-1">1. Structural Node Allocations (<code>is_ast_enabled</code>) <a class="header-anchor" href="#_1-structural-node-allocations-is-ast-enabled" aria-label="Permalink to &quot;1. Structural Node Allocations (`is_ast_enabled`)&quot;">​</a></h2><p>For helper or temporary variables in the grammar (like <code>_StringContent</code> or <code>_OptionalBlank</code> starting with an underscore), AST node generation should be skipped completely to keep memory usage low.</p><ul><li><p><strong>LL Parser (Top-Down):</strong> LL parses rules top-down. The parser knows which rule it is expanding before it matches the children. If the variable has <code>is_ast_enabled = False</code>, LL simply skips allocating the AST node.</p></li><li><p><strong>LR Parser (Bottom-Up):</strong> LR parses rules bottom-up, meaning parent nodes are only created during reduction. If the reduced variable has <code>is_ast_enabled = False</code>, LR skips the node allocation. However, because it still needs to track positions for parent rules, it pushes the start position (<code>context.pos()</code>) directly onto the semantic stack as a raw integer instead of allocating an AST node.</p></li></ul><hr><h2 id="_2-terminal-ast-allocations-ast-for-terminals" tabindex="-1">2. Terminal AST Allocations (<code>ast-for-terminals</code>) <a class="header-anchor" href="#_2-terminal-ast-allocations-ast-for-terminals" aria-label="Permalink to &quot;2. Terminal AST Allocations (`ast-for-terminals`)&quot;">​</a></h2><p>When <code>--ast-for-terminals</code> is enabled, the parser is instructed to create AST leaf nodes for terminal tokens. However, terminal tokens inside AST-suppressed helper variables (like characters inside a string) should still be ignored.</p><h3 id="ll-parser-generated-ast-suppressed-variants" tabindex="-1">LL Parser: Generated AST-Suppressed Variants <a class="header-anchor" href="#ll-parser-generated-ast-suppressed-variants" aria-label="Permalink to &quot;LL Parser: Generated AST-Suppressed Variants&quot;">​</a></h3><p>Because LL is generated top-down, the generator propagates a <code>skip_ast_construction</code> decision while emitting parser functions:</p><ol><li>When a child belongs to an AST-suppressed helper rule, the generator selects an AST-suppressed parser variant.</li><li>The generator propagates that decision while emitting the helper&#39;s descendants.</li><li>Those generated variants omit structural and terminal AST allocations; no runtime boolean is needed.</li></ol><h3 id="lr-parser-static-analysis-constraint" tabindex="-1">LR Parser: Static Analysis Constraint <a class="header-anchor" href="#lr-parser-static-analysis-constraint" aria-label="Permalink to &quot;LR Parser: Static Analysis Constraint&quot;">​</a></h3><p>Because LR is bottom-up, it is built on a flat state machine rather than a call stack. At the moment the parser shifts a terminal token onto the stack, it has no top-down call stack context to know whether that token belongs to an AST-enabled parent or an AST-suppressed helper variable.</p><p>To solve this limitation, LR uses a <strong>compile-time static analysis</strong> mechanism:</p><ol><li>During generator time, the generator builds a set of <code>linked_terminals</code> — terminals that appear on the RHS of at least one AST-enabled rule.</li><li>During runtime shifting, LR only allocates a terminal AST node if the terminal is in <code>linked_terminals</code>. Otherwise, it pushes the raw position <code>context.pos()</code>.</li><li><strong>Limitation:</strong> If a terminal (like <code>digit</code>) is linked in one rule (<code>IntegerNumber</code>) but not in another (<code>_PositiveIntegerNumberTail</code>), LR is forced to allocate nodes for it every time it shifts, because it cannot distinguish them bottom-up. LL, conversely, can select an AST-suppressed parser variant for <code>_PositiveIntegerNumberTail</code> during generation.</li></ol>',19)])])}const m=a(i,[["render",n]]);export{u as __pageData,m as default};
