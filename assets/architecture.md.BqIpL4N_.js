import{_ as a,o as t,c as r,a1 as o}from"./chunks/framework.BwhMLafE.js";const u=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"architecture.md","filePath":"architecture.md"}'),i={name:"architecture.md"};function n(s,e,l,c,d,h){return t(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><h2 id="table-of-contents" tabindex="-1">Table of Contents <a class="header-anchor" href="#table-of-contents" aria-label="Permalink to &quot;Table of Contents&quot;">​</a></h2><ul><li><a href="#overview">Overview</a></li><li><a href="#unified-no-lexer-design">Unified No-Lexer Design</a></li><li><a href="#native-call-stack-execution">Native Call-Stack Execution</a></li><li><a href="#syntax-error-recovery">Syntax-Error Recovery</a></li><li><a href="#verbatim-raw-capture">Verbatim Raw Capture (<code>@&gt;&gt;</code> / <code>@&gt;^&quot;...&quot;</code> / <code>@&gt;&quot;...&quot;^</code>)</a></li><li><a href="#optional-stack-overflow-recovery">Optional Stack-Overflow Recovery</a></li><li><a href="#dense-integer-node-pooling">Dense Integer Node Pooling</a></li><li><a href="#self-repeating-decisions">Self-Repeating Decisions</a></li><li><a href="#ambiguity-diagnostics">Ambiguity Diagnostics</a></li><li><a href="#role-of-the-self-hosted-generator">Role of the Self-Hosted Generator</a></li><li><a href="#self-hosting">Self-Hosting</a></li></ul><hr><h2 id="overview" tabindex="-1">Overview <a class="header-anchor" href="#overview" aria-label="Permalink to &quot;Overview&quot;">​</a></h2><p>Galley generates LL and LR parsers as native Zig source, encoding grammar rules directly into code paths rather than interpreting transition tables at runtime. It pairs that with a design centered on that goal: a single no-lexer pass that matches characters and reduces rules at the same time, the native call stack as the parse stack in both recursive-descent and recursive-ascent parsers, AST node allocation decided at generation time per symbol, and a self-hosted generator that runs ahead-of-time to emit both LL and LR parser source from a parsed grammar. None of these ideas are unique to Galley on their own, but the combination is its take on parser generation.</p><hr><h2 id="unified-no-lexer-design" tabindex="-1">Unified No-Lexer Design <a class="header-anchor" href="#unified-no-lexer-design" aria-label="Permalink to &quot;Unified No-Lexer Design&quot;">​</a></h2><p>Traditional parsers split execution into two passes: a lexer (tokenizer) that scans source text and allocates token objects on the heap, followed by a parser that consumes those tokens.</p><p>Galley eliminates the separate lexer pass entirely. Character matching and structural grammar reduction happen simultaneously in a single, unified pass over the source byte buffer, avoiding the token-allocation and intermediate-buffering overhead of a separate lexer.</p><hr><h2 id="native-call-stack-execution" tabindex="-1">Native Call-Stack Execution <a class="header-anchor" href="#native-call-stack-execution" aria-label="Permalink to &quot;Native Call-Stack Execution&quot;">​</a></h2><p>In both generated LL recursive-descent and LR recursive-ascent parsers, Galley leverages the native CPU execution call stack as the grammar parsing stack.</p><p>Instead of dynamically allocating stack frame objects or pushing/popping state IDs in an array loop, grammar transitions compile directly into native machine function calls (<code>call</code> and <code>ret</code> instructions). This lets modern CPUs make use of their hardware return address stacks (RAS) and branch prediction units, avoiding the per-token dispatch and stack-array bookkeeping of table-driven parsers.</p><hr><h2 id="syntax-error-recovery" tabindex="-1">Syntax-Error Recovery <a class="header-anchor" href="#syntax-error-recovery" aria-label="Permalink to &quot;Syntax-Error Recovery&quot;">​</a></h2><p>Generated LL and LR parsers are fail-fast by default, returning <code>ParseError.SyntaxError</code> on the first mismatch. Enabling recovery produces either automatic or explicit (<code>@</code>-annotated) recovery modes, both built on the parser&#39;s native execution shape rather than a second parser stack. Recovery-enabled parsers stop after 10 syntax errors by default.</p><p>LL parsers also report the innermost-first sequence of variables being parsed at a syntax error, rendered as <code>Symbol &lt;~ RightHandSide &lt;~ ...</code>. The instrumentation is optional and folds away at compile time when disabled.</p><p>See <a href="/galley/syntax_error_recovery.html">Syntax-Error Recovery and Messages</a> for the full mechanics of both topics.</p><hr><h2 id="verbatim-raw-capture" tabindex="-1">Verbatim Raw Capture (<code>@&gt;&gt;</code> / <code>@&gt;^&quot;...&quot;</code> / <code>@&gt;&quot;...&quot;^</code>) <a class="header-anchor" href="#verbatim-raw-capture" aria-label="Permalink to &quot;Verbatim Raw Capture (`@&gt;&gt;` / `@&gt;^&quot;...&quot;` / `@&gt;&quot;...&quot;^`)&quot;">​</a></h2><p>An RHS occurrence annotated with the verbatim marker <code>@&gt;&gt;</code> or a literal terminator (<code>@&gt;^&quot;...&quot;</code> / <code>@&gt;&quot;...&quot;^</code>) captures every raw byte until a terminator reappears, without lexing, indentation translation, or escape decoding. The <code>^</code> position chooses whether the terminator stays in the input or is appended to the captured span.</p><p>The LL and LR generators both support it, with LR emitting verbatim reductions as default actions on all lookaheads because a captured body may begin with any byte.</p><p>See <a href="/galley/grammar_guidelines.html#8-verbatim-raw-capture">Grammar Guidelines §8</a> for the full syntax and semantics.</p><hr><h2 id="optional-stack-overflow-recovery" tabindex="-1">Optional Stack-Overflow Recovery <a class="header-anchor" href="#optional-stack-overflow-recovery" aria-label="Permalink to &quot;Optional Stack-Overflow Recovery&quot;">​</a></h2><p>Generated parsers use the native call stack, so excessive recursive nesting can exhaust the stack available to the calling thread. On Linux and macOS, Galley can run a parse inside a protected signal-recovery scope that converts a fault at the thread&#39;s stack boundary into <code>ParseError.StackOverflow</code>.</p><p>Recovery is disabled by default because establishing that scope adds fixed setup and teardown work to every protected parse call. Runtime callers opt in with <code>ParseOptions.stack_overflow_recovery = true</code>.</p><p>The recovery scope installs an alternate signal stack and temporary <code>SIGSEGV</code>/<code>SIGBUS</code> handlers, records the current thread&#39;s stack bounds, and restores the previous process and thread state when parsing finishes. A memory fault outside the active thread&#39;s stack boundary is forwarded to the previously installed handler instead of being reported as parser stack overflow.</p><hr><h2 id="dense-integer-node-pooling" tabindex="-1">Dense Integer Node Pooling <a class="header-anchor" href="#dense-integer-node-pooling" aria-label="Permalink to &quot;Dense Integer Node Pooling&quot;">​</a></h2><p>When AST construction is enabled, Galley avoids allocating individual nodes via the system heap (<code>malloc</code>). Instead, nodes are allocated from the <code>ASTAllocator</code>&#39;s node storage, which never relocates: on platforms with lazy-commit anonymous mappings (macOS, Linux, the BSDs) one contiguous address-space region is reserved up front and backed by the OS on first touch; other platforms use fixed-size segments that are allocated once and never moved.</p><p>Furthermore, AST nodes reference their parents, children, and siblings using integer indices rather than memory pointers. Because the storage never relocates, element addresses are stable for the allocator&#39;s lifetime: pointers resolved from an address (for example <code>args.currentNode()</code> inside a procedure hook) remain valid across subsequent node allocations. Session reuse retains the allocated storage, although reset currently clears the previously used node range before rewinding it.</p><p>AST allocation is also decided at generation time, per symbol: helper variables (written with a leading <code>_</code>) never allocate nodes, and the parser is emitted with exactly the node creation it needs, so no runtime branching decides whether to build a node. See <a href="/galley/ast_node_allocations.html">AST Node Allocations</a> for the full mechanics and the LR generator&#39;s static-analysis constraints.</p><hr><h2 id="self-repeating-decisions" tabindex="-1">Self-Repeating Decisions <a class="header-anchor" href="#self-repeating-decisions" aria-label="Permalink to &quot;Self-Repeating Decisions&quot;">​</a></h2><p>Rules that repeat a variable on their own right-hand side (list and suffix shapes) are recognized statically during planning. Instead of re-parsing the repeated variable from scratch each time, the generator emits a dedicated decision that steps through the repetition and stops on the first token that no longer matches, folding the loop into the parse flow.</p><hr><h2 id="ambiguity-diagnostics" tabindex="-1">Ambiguity Diagnostics <a class="header-anchor" href="#ambiguity-diagnostics" aria-label="Permalink to &quot;Ambiguity Diagnostics&quot;">​</a></h2><p>When the LL planner finds two productions of a variable that share a terminal, it reports the conflict together with the derivation chain that explains each side: the reason rules that placed the terminal into FIRST or FOLLOW, the nullable derivations that let it pass through, and where the terminal is finally produced. A left-factored rewrite is suggested when the conflicting productions share a prefix. See <a href="/galley/grammar_guidelines.html#7-operator-precedence--ambiguity-free-expression-extraction">Grammar Guidelines §7</a> for the reported output.</p><hr><h2 id="role-of-the-self-hosted-generator" tabindex="-1">Role of the Self-Hosted Generator <a class="header-anchor" href="#role-of-the-self-hosted-generator" aria-label="Permalink to &quot;Role of the Self-Hosted Generator&quot;">​</a></h2><p>The grammar analysis engine is self-hosted in Zig. Galley ships an LL seed parser for its own grammar format in <code>languages/galley/_ll-parser.zig</code>. The seed parser constructs the grammar model; the generator API then:</p><ol><li>Validates the parsed grammar model.</li><li>Computes FIRST, FOLLOW, and nullable sets.</li><li>Constructs deterministic LL(k) lookup tables or LR/LALR shift-reduce automata.</li><li>Emits optimized Zig parser source (<code>_ll-parser.zig</code> or <code>_lr-parser.zig</code>).</li></ol><p>Because this step happens entirely ahead-of-time (AOT), the runtime Zig binary carries zero generator overhead. The original Python bootstrap generator was removed after commit <code>0190e40</code>.</p><hr><h2 id="self-hosting" tabindex="-1">Self-Hosting <a class="header-anchor" href="#self-hosting" aria-label="Permalink to &quot;Self-Hosting&quot;">​</a></h2><p>Galley ships with a formal specification of its own grammar syntax (<code>languages/galley</code>). The tracked LL seed parser parses <code>.grm</code> files into the grammar model used by the generator API, which can emit both LL and LR parser source. The Galley LR parser stays generated/ignored and is used as a verification path rather than as a second bootstrap artifact.</p>',48)])])}const g=a(i,[["render",n]]);export{u as __pageData,g as default};
