[package]
name = "galley-bindings-test-fixture"
version = "0.0.0"
edition = "2021"
description = "Test-only package exercising the Galley Rust bindings against their own fixture grammar"
license = "MIT"

[dependencies]
galley = { path = ".." }

[build-dependencies]
galley = { path = ".." }
