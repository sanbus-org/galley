# TypeScript Bindings

`koffi`-based FFI bindings over `bindings/c/galley.h`.

See `docs/bindings_typescript.md` and `examples/typescript` for the consumer flow.

One shared library embeds one parser; sessions are not thread-safe.
