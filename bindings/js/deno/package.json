{
  "name": "@sanbus/galley-deno",
  "version": "0.0.0",
  "description": "Galley JavaScript bindings for Deno (@sanbus/galley-core over Deno.dlopen and bindings/c/galley.h)",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": ["dist", "src", "build.ts", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json --noCheck --types \"\"",
    "prepare": "tsc -p tsconfig.build.json --noCheck --types \"\"",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@sanbus/galley-core": "file:../core"
  }
}
