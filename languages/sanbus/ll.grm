Start
| NewLineTail Rules

Rules
| Rule RulesTail

RulesTail
| OptionalNewLineMany Rule RulesTail
|

Rule
| UppercaseId RuleTail

RuleTail
| ".:" EnumBody
| ":" ActionBody

EnumBody
| BlockStart EnumItems BlockEnd

EnumItems
| EnumItemRow EnumItemsTail

EnumItemsTail@dropIfEmpty
| NewLine EnumItemRow EnumItemsTail
|

EnumItemRow
| "-" space UppercaseId

ActionBody
| BlockStart Fields BlockEnd ActionBodyTail
| NewLine "---" WrappedLogicBody

ActionBodyTail@dropIfEmpty
| "---" WrappedLogicBody
|

Fields
| FieldRow FieldsTail

FieldsTail@dropIfEmpty
| NewLine FieldRow FieldsTail
|

FieldRow
| "-" space LowercaseId ":" OptionalBlank Type OptionalFieldDefaultValue

OptionalFieldDefaultValue@dropIfEmpty@replaceWithChildren
| OptionalBlank "=" OptionalBlank Expression
|

Type
| Id OptionalTypeArray

OptionalTypeArray
| "[]"
|

ActionOutcomeEntry
| StateOutcome
| ActionDispatchOutcome
| SideEffects

StateOutcome
| ">state:" space State

State
| LowercaseId ":" UppercaseId InstantiationTail
| UppercaseId InstantiationTail
| "*" InstantiationTail

ActionDispatchOutcome
| ">actions:" BlockStart ActionsToDispatch BlockEnd

ActionsToDispatch
| NewLine "-" space Expression ActionsToDispatch
| "-" space Expression

SideEffects
| ">effects:" BlockStart SideEffectsToDispatch BlockEnd

SideEffectsToDispatch
| NewLine "-" space Expression SideEffectsToDispatch
| "-" space Expression

WrappedLogicBody
| BlockStart LogicBody BlockEnd

LogicBody
| Expression LogicBodyTail

LogicBodyTail@dropIfEmpty
| NewLine LogicBody
|

Expression
| OperandAndNumber OptionalBlank ExpressionTail
| ActionOutcomeEntry

ExpressionTail@dropIfEmpty
| Operator OptionalBlank Expression
|

OperandAndNumber
| WrappedLogicBody
| Number
| Operand OperandTail

Operand
| LowercaseId OperandLowercaseIdTail
| UppercaseId OperandUppercaseIdTail
| UnderscoreId
| String
| List

OperandTail@dropIfEmpty
| "[" ListGetTail
|

ListGetTail
| Expression ListGetExpressionTail
| "..]"
| ".." Expression "]"

ListGetExpressionTail
| "]"
| "..]"
| ".." Expression "]"

OperandLowercaseIdTail@replaceWithChildren
| "." Id
| "(" Parameters ")"
| "()"
| ":" UppercaseId CompoundIdTail
|

CompoundIdTail
| "." Id
| InstantiationTail
|

OperandUppercaseIdTail
| InstantiationTail
|

InstantiationParameters
| InstantiationParameter InstantiationParametersTail

InstantiationParametersTail
| NewLine InstantiationParameter InstantiationParametersTail
|

InstantiationParameter
| Id OptionalBlank "=" OptionalBlank Expression

InstantiationTail
| "(" BlockStart InstantiationParameters BlockEnd ")"
| "()"

Parameters
| OptionalBlank Expression ParametersTail
|

ParametersTail
| "," OptionalBlank Expression ParametersTail
|

List
| "[" ListItems "]"

ListItems
| Expression ListItemsTail
| "*" Expression
|

ListItemsTail
| "," space NonEmptyListItems
| "<|" Id "|" space Expression
|

NonEmptyListItems
| Expression NonEmptyListItemsTail
| "*" Expression

NonEmptyListItemsTail
| "," space NonEmptyListItems
|

String
| "\u{22}" StringContent "\u{22}"

StringContent
| character^"\u{22}" StringContent
|

Operator
| operator
| "{" Expression "}"

Blank
| space
| "	"

OptionalBlank
| Blank OptionalBlank
|

ForceBlank
| Blank OptionalBlank

OptionalNewLineMany
| NewLine OptionalNewLineMany
|

Id
| LowercaseId
| UppercaseId
| UnderscoreId

IdTail
| letter IdTail
| digit IdTail
| "_" IdTail
|

UnderscoreId
| "_" IdTail

LowercaseId
| lowercase_letter IdTail

UppercaseId
| uppercase_letter IdTail

Number
| IntegerNumber FloatTail

IntegerNumber
| PositiveIntegerNumber
| NegativeIntegerNumber

PositiveIntegerNumber
| digit PositiveIntegerNumberTail

PositiveIntegerNumberTail
| digit PositiveIntegerNumberTail
|

NegativeIntegerNumber
| "-" PositiveIntegerNumber

FloatTail
| "." PositiveIntegerNumber
|

NewLine
| new_line NewLineTail

BlockStart
| block_start NewLineTail

BlockEnd
| block_end NewLineTail

NewLineTail
| "#"!>^"\n" new_line NewLineTail
|
